#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <stdarg.h>
#include <unistd.h>

int _putchar(char z);
int _printf(const char *format, ...);
int print_string(const char *str);
int print_int(int num);

#endif
