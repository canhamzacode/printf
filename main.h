#ifndef _MAIN_H_
#define _MAIN_H_

#include <stdio.h>
#include <string.h>
#include <unistd.h>
#include <stdarg.h>
int _printf(const char *format, ...);
void print_string(const char *str);
void print_char(const int ch);

#endif